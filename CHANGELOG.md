# 更新日志

## 2026-08-08 R12.5

### 修正

- 固定托管 Sub-Store `2.36.31` 的 Core 与 Simple 重写脚本，移除 GitHub Release 运行时下载依赖。
- 将 `[Script]` 路径改为固定提交的 jsDelivr 地址，避免 Release 跳转导致的 TLS 失败和 `AllServer` 解析失败。
- 增加脚本来源说明、许可证副本、校验和及发布清单记录。

## 2026-08-08 R12.4

### 修正

- 将 Sub-Store 的 `sub.store` 请求重写直接内置到主配置的 `[MITM]` 与 `[Script]`。
- 保留唯一 `YOUR_SUBSTORE_SURGE_URL` 占位符；无需另建策略组或重复安装模块。
- 同步更新审计器、回归测试、锁文件、README、发布清单和校验文件。

## 2026-08-08 R12.3

### 修正

- 将 Surge 自身的加密 DNS 从海外 IP 端点改为国内可达 DoH，避免中国移动等网络出现 `NSURLErrorDomain:-1001`。
- 同步更新 DNS 审计、锁文件、回归测试、README、发布清单和校验文件。

## 2026-08-08 R12.2

### 修正

- 将订阅入口固定为 `[Proxy Group]` 中 `AllServer` 的单个 `policy-path` 占位符。
- 只需替换 `YOUR_SUBSTORE_SURGE_URL`，节点会由 `AllServer` 自动拉取并交给地区组筛选。
- 移除 `sub.store = 127.0.0.1` Host 映射，避免远程订阅请求被送到本机而返回 404。
- 公开文件不写入真实订阅地址，审计器只允许保留占位符，真实 URL 必须留在 Surge 本地。
- 同步更新 README、锁文件、审计器、回归测试、发布清单和完整 ZIP。

## 2026-08-08 R12.1

> 历史版本说明。当前 R12.2 已改为单个订阅占位符填写方式。

### 修正

- 移除 `AllServer` 对占位 `policy-path` 和 `sub.store` 短链的运行时依赖。
- 改为自动收集已导入当前配置 `[Proxy]` 区域的节点，避免订阅预览正常但策略组全部失败。
- 放宽代理测速等待时间至 8 秒，减少移动网络下的误判。
- 删除宽泛的全局订阅关键词过滤，避免合法节点被误删；地区组仍按地区名称筛选。
- 补齐审计器、锁文件、说明文档和回归测试中的订阅接入约束。

## 2026-08-08 R12

### 修正

- APNs 改为独立 `ApplePush` Fallback，代理优先、直连故障回落。
- 启用 `include-all-networks` 与 `include-apns`，覆盖移动数据下的系统推送。
- 加入 APNs 本地快照，并同步嵌入 `Surge.conf`，避免运行时依赖远程规则。
- 加密 DNS 使用 Cloudflare 与 Quad9 IP 端点，Surge 自身请求直连，避免域名节点造成解析循环。
- 增加 `223.5.5.5` 与 `114.114.114.114` 作为 DNS 引导地址。
- 保持 Telegram 强制代理及既有国内外分流，不引入全量 Apple 代理规则。
- 修正 `ChinaDomain` 位于 AdBlock 前导致的广告规则优先级问题。
- 补充订阅节点筛选和策略组审计。

### 校验

- 审计器、锁文件、回归测试和工作流统一升级为 R12。
- 增加发布文件清单，发布包按清单核对文件数量与 SHA-256。

## 2026-08-01 R11 LTS

### 新增

- 新增仓库级 `LICENSE`。
- 新增 `tools/generate_checksums.py`，统一生成发布文件校验和。
- GitHub Actions 增加 Python 3.12 与 3.13 双版本审计。
- README 增加完整目录、工具、工作流、FAQ 和故障排查说明。

### 优化

- 配置注释统一为简短文字标题，不使用装饰性横线。
- 统一审计脚本、锁文件、文档和工作流的 R11 LTS 版本标识。
- `audit_rules.py` 支持验证仓库规则目录和 ZIP 暂存规则目录。
- 完善 `.gitignore`，排除缓存、临时文件、压缩包和本地敏感配置。
- 工作流增加并发控制、超时限制、编译检查和 SHA-256 校验。

### 保持

- Telegram 强制代理。
- 系统 APNs 不由 Surge VIF 接管。
- APNs 精确直连兜底。
- `FINAL,Final,dns-failed` 失败关闭。
- 5546 条有效规则及既有规则顺序。

## 2026-08-01 R10.6

- 修复 Telegram 后台通知与 APNs 路由冲突。
- 补充 Telegram 核心网段。
- 清理旧校验和记录。

## 2026-07-31 R10.5

- 启用 AliDNS 与 DNSPod DoH。
- 增加 DNS 引导映射和防绕过规则。
- 同步配置审计、规则锁和回归测试。
